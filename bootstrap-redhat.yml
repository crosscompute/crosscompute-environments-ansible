---
- name: bootstrap ansible
  hosts: all
  gather_facts: false
  become: true
  tasks:
    - name: install packages for ansible
      raw: test -e /usr/bin/python || dnf -y install python2-dnf
      changed_when: False
